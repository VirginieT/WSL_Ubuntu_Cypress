explo = explorer.exe .
upgd = sudo apt update && sudo apt upgrade
smsq = sudo mysql -u root -p
sss = symfony server:start
phps = 'php -S 127.0.0.1:8000 -t public'
sy = 'php bin/console'
gac = git add . && git commit -m
gp = 'git push'

